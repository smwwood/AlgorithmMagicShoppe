# The Magical Miscellany

A small, responsive classroom game built with plain HTML, CSS, and JavaScript.

## Run locally

Open `index.html` directly, or serve the folder with any static web server.

## GitHub Pages

In the repository settings, open **Pages**, choose **Deploy from a branch**, and select the repository's default branch and the root folder.

No build step, backend, account, or persistent storage is required.
